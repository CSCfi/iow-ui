describe('myApp.model module', function() {

  beforeEach(module('myApp.model'));

  describe('model controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var modelCtrl = $controller('ModelCtrl');
      expect(modelCtrl).toBeDefined();
    }));

  });
});
